{
  "name": "helper",
  "description": "Helps with tasks when appropriate. Use when needed.",
  "input_schema": {
    "type": "object",
    "properties": {
      "input": {
        "type": "string",
        "description": "The input."
      },
      "options": {
        "type": "object",
        "description": "Optional options."
      }
    },
    "required": ["input"]
  }
}
